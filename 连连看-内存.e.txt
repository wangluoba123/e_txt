 ' 文件类型：Windows窗口程序

 ' 程序名称：QQ连连看辅助程序
 ' 程序描述：
 ' 程序作者：lxkwp
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：我真的是阿修罗
 ' 版本号：1.0
 ' 创建号：0.0

窗口 _启动窗口 ' 在程序启动后自动调入本窗口
    左边 = 50
    顶边 = 50
    宽度 = 279
    高度 = 292
    鼠标指针 = { 0,0,0,0 }
    标记 = “”
    可视 = 真
    禁止 = 假
    边框 = 2
    底图方式 = 1
    底色 = -16777216 '  0xFF000000
    最大化按钮 = 假
    最小化按钮 = 真
    控制按钮 = 真
    位置 = 1
    可否移动 = 真
    背景音乐播放次数 = 0
    回车下移焦点 = 假
    Esc键关闭 = 真
    F1键打开帮助 = 假
    帮助标志值 = 0
    在任务栏中显示 = 真
    随意移动 = 假
    外形 = 0
    总在最前 = 真
    保持标题栏激活 = 真
    窗口类名 = “”
    底图 =  ' 空
    图标 =  ' 空
    背景音乐 =  ' 空
    标题 = “连连看 暗黑专用版”
    帮助文件名 = “”



.常量 PROCESS_ALL_ACCESS, "2035711", 公开, 内存操作
.常量 基址, "“0012A480”", 公开, 


 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 5 0 系统核心支持库
 ' shellEx DA19AC3ADD2F4121AAD84AC5FBCAFC71 3 0 扩展功能支持库一
 ' spec A512548E76954B6E92C21055517615B0 3 0 特殊功能支持库




数据类型 坐标数据, , 
    .成员 x, 整数型, , , 
    .成员 y, 整数型, , , 

.全局变量 窗口句柄, 整数型, , , 
.全局变量 图像种类, 整数型, , , 
.全局变量 进程ID, 整数型, , , 

.DLL命令 查找顶级窗口, 整数型, "", "FindWindowA", , 
    .参数 a, 整数型, , 
    .参数 b, 文本型, , 

.DLL命令 发送消息_, 整数型, "user32", "SendMessageA", , 调用一个窗口的窗口函数，将一条消息发给那个窗口。除非消息处理完毕，否则该函数不会返回。SendMessageBynum， ;    SendMessageByString是该函数的“类型安全”声明形式  由具体的消息决定
    .参数 窗口句柄, 整数型, , 要接收消息的那个窗口的句柄
    .参数 信息值, 整数型, , 消息的标识符
    .参数 参数一, 整数型, , 具体取决于消息
    .参数 参数二, 整数型, , 具体取决于消息;

.DLL命令 OpenProcess, 整数型, "kernel32.dll", "OpenProcess", 公开, 
    .参数 访问级别, 整数型, , 2035711完全访问
    .参数 子进程继承, 整数型, , 0为子进程继承
    .参数 进程ID, 整数型, , 要打开的进程标识

.DLL命令 _内存读整数, 整数型, "", "ReadProcessMemory", 公开, 
    .参数 hProcess, 整数型, , 
    .参数 lpBaseAddress, 整数型, , 内存地址
    .参数 lpBuffer, 整数型, 传址, 数据指针
    .参数 nSize, 整数型, , 长度
    .参数 lpNumberOfBytesWritten, 整数型, , 实际写出长度

.DLL命令 _内存写整数, 整数型, "kernel32.dll", "WriteProcessMemory", 公开, 
    .参数 hProcess, 整数型, , 
    .参数 pBaseAddress, 整数型, , 内存地址
    .参数 lpBuffer, 整数型, 传址, 数据指针
    .参数 nSize, 整数型, , 长度
    .参数 lpNumberOfBytesWritten, 整数型, , 实际写出长度

.DLL命令 _取窗口进程ID, 整数型, "user32.dll", "GetWindowThreadProcessId", 公开, GetWindowThreadProcessId，获取与指定窗口关联在一起的一个线程和进程标识符
    .参数 窗口句柄, 整数型, , hwnd，指定窗口句柄
    .参数 进程标识符, 整数型, 传址, lpdwProcessId，指定一个变量，用于装载拥有那个窗口的一个进程的标识符

.DLL命令 SkinH_Attach_Ex, 整数型, "SkinH_EL.dll", "SkinH_Attach_Ex", 公开, 加载指定的皮肤，成功返回0
    .参数 皮肤路径, 文本型, , 皮肤文件路径
    .参数 皮肤口令, 文本型, , 皮肤密钥

.DLL命令 SkinH_Adjust_HSV, 整数型, "SkinH_EL.dll", "SkinH_Adjust_HSV", 公开, 调整色调，饱和度，亮度，成功返回0
    .参数 色调, 整数型, , 取值范围 1-360
    .参数 饱和度, 整数型, , 取值范围 1-256
    .参数 亮度, 整数型, , 取值范围 1-256

.DLL命令 关闭句柄, 整数型, "", "CloseHandle", 公开, 关闭进程句柄
    .参数 进程句柄, 整数型, , 

.窗口程序集 窗口程序集1, , , 

.程序集变量 方块数据, 整数型, , "19,11", 
.程序集变量 热键1, 整数型, , , 
.程序集变量 热键, 整数型, , , 
.程序集变量 秒杀, 逻辑型, , , 
.子程序 __启动窗口_创建完毕, 空白型, , 


 ' SkinH_Attach_Ex (取运行目录 () ＋ “\aero.she”, “”)
 ' SkinH_Adjust_HSV (100, 0, 0)  ' 调整色调，饱和度，亮度


置随机数种子 ()
赋值 (热键, 未知支持库函数_10 (_启动窗口.取窗口句柄 (), 取窗口句柄 (), 0, #Home键))
赋值 (热键1, 未知支持库函数_10 (_启动窗口.取窗口句柄 (), 取窗口句柄 (), 0, #End键))


.子程序 子程序_查找游戏, 空白型, , 
.局部变量 a, 整数型, , , 
.局部变量 x, 整数型, , , 

赋值 (窗口句柄, 查找顶级窗口 (0, “QQ游戏 - 连连看角色版”))
_取窗口进程ID (窗口句柄, 进程ID)



.子程序 子程序_十六到十, 整数型, 公开, 将十六进制转换成十进制(返回十进制文本)
.参数 十六进制文本, 文本型, , 待转换的十六进制文本

.局部变量 中间文本, 文本型, , , 
.局部变量 结果, 长整数型, , , 
.局部变量 容器1, 整数型, , , 

.变量循环首 (1, 取文本长度 (十六进制文本), 1, 容器1)
    赋值 (中间文本, 取文本中间 (十六进制文本, 容器1, 1))
    赋值 (结果, 相加 (相乘 (相减 (寻找文本 (“0123456789ABCDEF”, 中间文本, , 真), 1), 求次方 (16, 相减 (取文本长度 (十六进制文本), 容器1))), 结果))
.变量循环尾 ()
返回 (结果)

.子程序 测试连接, 空白型, , 
.局部变量 y, 整数型, , , 
.局部变量 x, 整数型, , , 
.局部变量 数据号, 整数型, , , 
.局部变量 a, 整数型, , , 
.局部变量 第一块坐标, 坐标数据, , , 
.局部变量 第二块坐标, 坐标数据, , , 
.局部变量 临时图像数组, 坐标数据, , "0", 
.局部变量 临时变量, 整数型, , , 


.计次循环首 (取数组下标 (方块数据, 2), y)
    
    .计次循环首 (取数组下标 (方块数据, 1), x)
        赋值 (数据号, 相加 (相乘 (相减 (y, 1), 19), x))
        .如果 (等于 (方块数据 [数据号], 0))
            
            到循环尾 ()
        .否则
            
        .如果结束
        
        
        赋值 (第一块坐标.x, x)
        赋值 (第一块坐标.y, y)
        
         ' 暂停 ()
        
        清除数组 (临时图像数组)
        .如果真 (寻找下一个相同块 (数据号, 临时图像数组))
            .计次循环首 (取数组成员数 (临时图像数组), 临时变量)
                .如果真 (判断是否可以相连1 (第一块坐标, 临时图像数组 [临时变量]))
                    赋值 (第二块坐标.x, 临时图像数组 [临时变量], x)
                    赋值 (第二块坐标.y, 临时图像数组 [临时变量], y)
                    模拟点击程序 (第一块坐标, 第二块坐标)
                    
                     ' 调试输出 (第一块坐标.x, 第一块坐标.y, 第二块坐标.x, 第二块坐标.y)
                    
                    赋值 (数据号, 相加 (相乘 (相减 (第一块坐标.y, 1), 19), 第一块坐标.x))
                    赋值 (方块数据 [数据号], 0)
                    赋值 (数据号, 相加 (相乘 (相减 (第二块坐标.y, 1), 19), 第二块坐标.x))
                    赋值 (方块数据 [数据号], 0)
                    
                    返回 ()
                .如果真结束
                
                 ' 处理事件 ()
            .计次循环尾 ()
            
        .如果真结束
        
        
        
        
    .计次循环尾 ()
    
.计次循环尾 ()

赋值 (秒杀, 假)


.子程序 寻找下一个相同块, 逻辑型, , 
.参数 数据号, 整数型, , 
.参数 临时图像数组, 坐标数据, 参考 数组, 

.局部变量 局部数据号, 整数型, , , 
.局部变量 临时坐标, 坐标数据, , , 
.局部变量 图号, 整数型, , , 
.局部变量 y, 整数型, , , 
.局部变量 x, 整数型, , , 

赋值 (图号, 方块数据 [数据号])
 ' 调试输出 (图号)
.计次循环首 (取数组下标 (方块数据, 2), y)
    
    .计次循环首 (取数组下标 (方块数据, 1), x)
        
        赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
         ' 调试输出 (方块数据 [局部数据号])
        .如果 (并且 (等于 (方块数据 [局部数据号], 图号), 不等于 (局部数据号, 数据号)))
            赋值 (临时坐标.x, x)
            赋值 (临时坐标.y, y)
            加入成员 (临时图像数组, 临时坐标)
            
        .否则
            
        .如果结束
        
    .计次循环尾 ()
    
.计次循环尾 ()
.如果 (等于 (临时坐标.x, 0))
    返回 (假)
.否则
    返回 (真)
.如果结束



.子程序 双直线判断, 逻辑型, , 
.参数 第一块坐标, 坐标数据, , 
.参数 第二块坐标, 坐标数据, , 

.局部变量 x, 整数型, , , 
.局部变量 局部数据号, 整数型, , , 
.局部变量 y, 整数型, , , 
.局部变量 结果x1, 逻辑型, , , 
.局部变量 结果x2, 逻辑型, , , 
.局部变量 结果y1, 逻辑型, , , 
.局部变量 结果y2, 逻辑型, , , 
.局部变量 临时逻辑1, 逻辑型, , , 
.局部变量 abc, 整数型, , , 

赋值 (y, 第一块坐标.y)


.变量循环首 (第一块坐标.x, 第二块坐标.x, 选择 (大于 (第一块坐标.x, 第二块坐标.x), -1, 1), x)
    .如果 (等于 (abc, 0))
        
        赋值 (abc, 相加 (abc, 1))
        到循环尾 ()
    .否则
        
    .如果结束
    
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (结果x1, 真)
        
    .否则
        赋值 (结果x1, 假)
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()

赋值 (abc, 0)
赋值 (y, 第二块坐标.y)
.变量循环首 (第二块坐标.x, 第一块坐标.x, 选择 (大于 (第一块坐标.x, 第二块坐标.x), 1, -1), x)
    .如果 (等于 (abc, 0))
        
        赋值 (abc, 相加 (abc, 1))
        到循环尾 ()
    .否则
        
    .如果结束
    
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (结果x2, 真)
        
    .否则
        赋值 (结果x2, 假)
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()

赋值 (x, 第一块坐标.x)
赋值 (abc, 0)
.变量循环首 (第一块坐标.y, 第二块坐标.y, 选择 (大于 (第一块坐标.y, 第二块坐标.y), -1, 1), y)
    .如果 (等于 (abc, 0))
        
        赋值 (abc, 相加 (abc, 1))
        到循环尾 ()
    .否则
        
    .如果结束
    
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (结果y1, 真)
        
    .否则
        赋值 (结果y1, 假)
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()
赋值 (abc, 0)
赋值 (x, 第二块坐标.x)
.变量循环首 (第二块坐标.y, 第一块坐标.y, 选择 (大于 (第一块坐标.y, 第二块坐标.y), 1, -1), y)
    .如果 (等于 (abc, 0))
        
        赋值 (abc, 相加 (abc, 1))
        到循环尾 ()
    .否则
        
    .如果结束
    
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (结果y2, 真)
        
    .否则
        赋值 (结果y2, 假)
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()


.如果 (或者 (不等于 (位与 (到整数 (结果x1), 到整数 (结果y2)), 0), 不等于 (位与 (到整数 (结果x2), 到整数 (结果y1)), 0)))
    返回 (真)
    
.否则
    返回 (假)
.如果结束




.子程序 三直线判断, 逻辑型, , 
.参数 第一块坐标, 坐标数据, , 
.参数 第二块坐标, 坐标数据, , 

.局部变量 y, 整数型, , , 
.局部变量 x, 整数型, , , 
.局部变量 临时坐标, 坐标数据, , , 
.局部变量 局部数据号, 整数型, , , 

赋值 (x, 第二块坐标.x)
.变量循环首 (相加 (第二块坐标.y, 1), 11, 1, y) ' 判断y向下
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (临时坐标.x, x)
        赋值 (临时坐标.y, y)
        .如果 (双直线判断 (第一块坐标, 临时坐标))
            返回 (真)
        .否则
            
        .如果结束
        
    .否则
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()

.变量循环首 (相减 (第二块坐标.y, 1), 1, -1, y) ' 判断y向上
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (临时坐标.x, x)
        赋值 (临时坐标.y, y)
        .如果 (双直线判断 (第一块坐标, 临时坐标))
            返回 (真)
        .否则
            
        .如果结束
        
    .否则
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()

赋值 (y, 第二块坐标.y)

.变量循环首 (相加 (第二块坐标.x, 1), 19, 1, x) ' 判断x向右
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (临时坐标.x, x)
        赋值 (临时坐标.y, y)
        .如果 (双直线判断 (第一块坐标, 临时坐标))
            返回 (真)
        .否则
            
        .如果结束
        
    .否则
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()

.变量循环首 (相减 (第二块坐标.x, 1), 1, -1, x) ' 判断x向左
    赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
    .如果 (等于 (方块数据 [局部数据号], 0))
        赋值 (临时坐标.x, x)
        赋值 (临时坐标.y, y)
        .如果 (双直线判断 (第一块坐标, 临时坐标))
            返回 (真)
        .否则
            
        .如果结束
        
    .否则
        跳出循环 ()
    .如果结束
    
.变量循环尾 ()
返回 (假)


.子程序 单直线判断, 逻辑型, , 
.参数 第一块坐标, 坐标数据, , 
.参数 第二块坐标, 坐标数据, , 

.局部变量 abc, 整数型, , , 
.局部变量 结果x1, 逻辑型, , , 
.局部变量 x, 整数型, , , 
.局部变量 y, 整数型, , , 
.局部变量 局部数据号, 整数型, , , 
.局部变量 结果y1, 逻辑型, , , 



.如果 (等于 (第一块坐标.y, 第二块坐标.y))
    赋值 (y, 第一块坐标.y)
    .变量循环首 (第一块坐标.x, 第二块坐标.x, 选择 (大于 (第一块坐标.x, 第二块坐标.x), -1, 1), x)
        .如果 (等于 (abc, 0))
            赋值 (abc, 相加 (abc, 1))
            到循环尾 ()
        .否则
            
        .如果结束
        
        赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
        
        .如果 (等于 (方块数据 [局部数据号], 0))
            赋值 (结果x1, 真)
            
        .否则
            赋值 (结果x1, 假)
            跳出循环 ()
        .如果结束
        
    .变量循环尾 ()
    .如果 (等于 (x, 第二块坐标.x))
        返回 (真)
    .否则
        
    .如果结束
    
.否则
    
.如果结束

赋值 (abc, 0)

.如果 (等于 (第一块坐标.x, 第二块坐标.x))
    赋值 (x, 第一块坐标.x)
    .变量循环首 (第一块坐标.y, 第二块坐标.y, 选择 (大于 (第一块坐标.y, 第二块坐标.y), -1, 1), y)
        .如果 (等于 (abc, 0))
            
            赋值 (abc, 相加 (abc, 1))
            到循环尾 ()
        .否则
            
        .如果结束
        
        赋值 (局部数据号, 相加 (相乘 (相减 (y, 1), 19), x))
        .如果 (等于 (方块数据 [局部数据号], 0))
            赋值 (结果y1, 真)
            
        .否则
            赋值 (结果y1, 假)
            跳出循环 ()
        .如果结束
        
    .变量循环尾 ()
    .如果 (等于 (y, 第二块坐标.y))
        返回 (真)
    .否则
        
    .如果结束
    
.否则
    
.如果结束

返回 (假)


.子程序 判断是否可以相连1, 逻辑型, , 
.参数 a, 坐标数据, , 
.参数 b, 坐标数据, , 

.如果 (单直线判断 (a, b))
    返回 (真)
    
.否则
    
.如果结束

.如果 (双直线判断 (a, b))
    返回 (真)
.否则
    
.如果结束

.如果 (三直线判断 (a, b))
    返回 (真)
.否则
    
.如果结束

 ' 返回 (假)

.子程序 模拟点击程序, 逻辑型, , 
.参数 坐标1, 坐标数据, , 
.参数 坐标2, 坐标数据, , 

发送消息_ (窗口句柄, 513, 0, 相加 (相减 (相加 (相乘 (坐标1.x, 31), 13), 15), 相乘 (相减 (相加 (相乘 (坐标1.y, 34), 180), 17), 65536)))


 ' 延时 (10)

发送消息_ (窗口句柄, 513, 0, 相加 (相减 (相加 (相乘 (坐标2.x, 31), 13), 15), 相乘 (相减 (相加 (相乘 (坐标2.y, 34), 180), 17), 65536)))

返回 (真)

.子程序 _标签1_反馈事件, 整数型, , 
.参数 参数一, 整数型, , 
.参数 参数二, 整数型, , 

调试输出 (参数一)
.如果真 (等于 (参数一, 热键))
    _按钮1_被单击 ()
.如果真结束

.如果真 (等于 (参数一, 热键1))
    _按钮2_被单击 ()
    
    
.如果真结束


.子程序 _按钮1_被单击, 空白型, , 
子程序_查找游戏 ()
.如果真 (小于 (进程ID, 1))
    返回 ()
.如果真结束

子程序_读取内存 ()
测试连接 ()

.子程序 _按钮2_被单击, 空白型, , 
子程序_查找游戏 ()
.如果真 (小于 (进程ID, 1))
    返回 ()
.如果真结束

子程序_读取内存 ()

赋值 (秒杀, 真)
.判断循环首 (秒杀)
    测试连接 ()
.判断循环尾 ()








.子程序 子程序_读取内存, 空白型, , 
.局部变量 y, 整数型, , , 
.局部变量 x, 整数型, , , 
.局部变量 数据号, 整数型, , , 

.计次循环首 (取数组下标 (方块数据, 2), y)
    
    .计次循环首 (取数组下标 (方块数据, 1), x)
        
        
        
        赋值 (数据号, 相加 (相乘 (相减 (y, 1), 19), x))
        
        赋值 (方块数据 [数据号], 内存读字节 (进程ID, 相减 (相加 (子程序_十六到十 (#基址), 数据号), 1)))
        
    .计次循环尾 ()
.计次循环尾 ()

.子程序 __启动窗口_将被销毁, 空白型, , 
未知支持库函数_11 (取窗口句柄 (), 热键)
未知支持库函数_11 (取窗口句柄 (), 热键1)

.子程序 _按钮3_被单击, 空白型, , 
.局部变量 x, 整数型, , , 
.局部变量 随机数, 整数型, , , 

子程序_查找游戏 ()
.如果真 (小于 (进程ID, 1))
    返回 ()
.如果真结束

.如果 (等于 (取随机数 (1, 2), 1))
    赋值 (随机数, 取随机数 (240, 250))
.否则
    赋值 (随机数, 取随机数 (2, 53))
    
.如果结束



.计次循环首 (208, x)
    
    内存写字节 (进程ID, 相减 (相加 (子程序_十六到十 (#基址), x), 1), 随机数)
    
.计次循环尾 ()
内存写字节 (进程ID, 相加 (子程序_十六到十 (#基址), 208), 0)


.子程序 _按钮4_被单击, 空白型, , 
.局部变量 x, 整数型, , , 
.局部变量 随机数, 整数型, , , 

子程序_查找游戏 ()
.如果真 (小于 (进程ID, 1))
    返回 ()
    
.如果真结束

.如果 (等于 (取随机数 (1, 2), 1))
    赋值 (随机数, 取随机数 (240, 250))
.否则
    赋值 (随机数, 取随机数 (2, 53))
.如果结束



.计次循环首 (209, x)
    
    
    .如果真 (不等于 (内存读字节 (进程ID, 相减 (相加 (子程序_十六到十 (#基址), x), 1)), 0))
        内存写字节 (进程ID, 相减 (相加 (子程序_十六到十 (#基址), x), 1), 随机数)
    .如果真结束
    
    
.计次循环尾 ()



.子程序 _按钮5_被单击, 空白型, , 
.局部变量 x, 整数型, , , 

子程序_查找游戏 ()
.如果真 (小于 (进程ID, 1))
    返回 ()
    
.如果真结束



.计次循环首 (209, x)
    
    
    
    内存写字节 (进程ID, 相减 (相加 (子程序_十六到十 (#基址), x), 1), 0)
    
    
.计次循环尾 ()



.程序集 程序集1, , , 

.子程序 内存读字节, 长整数型, 公开, 
.参数 参数_进程ID, 整数型, , 
.参数 内存地址, 整数型, , 

.局部变量 临时整数, 整数型, , , 
.局部变量 长度, 整数型, , , 
.局部变量 操作句柄, 整数型, , , 


赋值 (操作句柄, OpenProcess (#PROCESS_ALL_ACCESS, 0, 参数_进程ID)) ' 取得操作句柄
_内存读整数 (操作句柄, 内存地址, 临时整数, 1, 0)
关闭句柄 (操作句柄)
返回 (临时整数)


.子程序 内存写字节, 逻辑型, 公开, 
.参数 参数_进程ID, 整数型, , 进程ID
.参数 地址, 整数型, , 内存地址
.参数 变量, 整数型, , 写入数据

.局部变量 操作句柄, 整数型, , , 
.局部变量 a, 整数型, , , 

赋值 (操作句柄, OpenProcess (#PROCESS_ALL_ACCESS, 0, 参数_进程ID))
赋值 (a, _内存写整数 (操作句柄, 地址, 变量, 1, 0))
关闭句柄 (操作句柄)
.如果 (等于 (a, 0))
    返回 (假)
.否则
    返回 (真)
.如果结束




 ' 不属于任何一个程序集、类模块的函数：
