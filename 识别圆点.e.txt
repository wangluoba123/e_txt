 ' 文件类型：Windows窗口程序

 ' 程序名称：
 ' 程序描述：
 ' 程序作者：
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0

窗口 _启动窗口 ' 在程序启动后自动调入本窗口
    左边 = 50
    顶边 = 50
    宽度 = 579
    高度 = 537
    鼠标指针 = { 0,0,0,0 }
    标记 = “”
    可视 = 真
    禁止 = 假





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 5 0 系统核心支持库





.窗口程序集 窗口程序集1, , , 

.子程序 _按钮1_被单击, 空白型, , 
.局部变量 x, 整数型, , , 
.局部变量 y, 整数型, , , 
.局部变量 p, 字节集, , , 
.局部变量 椭圆数, 整数型, , , 

.变量循环首 (0, 相减 (., 1), 1, y)
    .变量循环首 (0, 相减 (., 1), 1, x)
        赋值 (p, 到字节集 (x.取点 (y)))
        .如果真 (并且 (小于 (p [1], 100), 小于 (p [2], 100), 小于 (p [3], 100)))
            .如果真 (画椭圆 (x, y))
                赋值 (椭圆数, 相加 (椭圆数, 1))
            .如果真结束
            
        .如果真结束
        
    .变量循环尾 ()
.变量循环尾 ()
信息框 (相加 (“共有椭圆个数：”, 到文本 (椭圆数)), 0, )

.子程序 画椭圆, 逻辑型, , 
.参数 x, 空白型, , 
.参数 y, 空白型, , 

.局部变量 x1, 整数型, , , 
.局部变量 y1, 整数型, , , 
.局部变量 中点y, 整数型, , , 
.局部变量 中点x, 整数型, , , 
.局部变量 x_1, 整数型, , , 
.局部变量 x_2, 整数型, , , 
.局部变量 y_1, 整数型, , , 
.局部变量 y_2, 整数型, , , 

赋值 (中点y, 0)
.变量循环首 (y, 相减 (., 1), 1, y1)
    .如果真 (是否为白色 (x.取点 (y1)))
        赋值 (中点y, 相除 (相减 (相加 (y, y1), 1), 2))
        跳出循环 ()
    .如果真结束
    
.变量循环尾 ()
.如果真 (等于 (中点y, 0))
    返回 (假)
.如果真结束
赋值 (x_1, 0)
.变量循环首 (x, 0, -1, x1)
    .如果真 (是否为白色 (x1.取点 (中点y)))
        赋值 (x_1, 相加 (x1, 1))
        跳出循环 ()
    .如果真结束
    
.变量循环尾 ()
.如果真 (等于 (x_1, 0))
    返回 (假)
.如果真结束
赋值 (x_2, 0)
.变量循环首 (x, 相减 (., 1), 1, x1)
    .如果真 (是否为白色 (x1.取点 (中点y)))
        赋值 (x_2, 相减 (x1, 1))
        跳出循环 ()
    .如果真结束
    
.变量循环尾 ()
.如果真 (等于 (x_2, 0))
    返回 (假)
.如果真结束
赋值 (中点x, 相除 (相加 (x_2, x_1), 2))
赋值 (y_1, 0)
.变量循环首 (中点y, 0, -1, y1)
    .如果真 (是否为白色 (中点x.取点 (y1)))
        赋值 (y_1, 相加 (y1, 1))
        跳出循环 ()
    .如果真结束
    
.变量循环尾 ()
.如果真 (等于 (y_1, 0))
    返回 (假)
.如果真结束
赋值 (y_2, 0)
.变量循环首 (中点y, 相减 (., 1), 1, y1)
    .如果真 (是否为白色 (中点x.取点 (y1)))
        赋值 (y_2, 相减 (y1, 1))
        跳出循环 ()
    .如果真结束
    
.变量循环尾 ()
.如果真 (等于 (y_2, 0))
    返回 (假)
.如果真结束
赋值 (., #红色)
赋值 (., #红色)
相减 (x_1, 2).画椭圆 (相减 (y_1, 2), 相加 (x_2, 2), 相加 (y_2, 2))
返回 (真)

.子程序 是否为白色, 逻辑型, , 
.参数 颜色, 整数型, , 

.局部变量 p, 字节集, , , 

.如果真 (或者 (等于 (颜色, #白色), 等于 (颜色, #红色)))
    返回 (真)
.如果真结束
赋值 (p, 到字节集 (颜色))
返回 (并且 (大于 (p [1], 100), 大于 (p [2], 100), 大于 (p [3], 100)))


 ' 不属于任何一个程序集、类模块的函数：
