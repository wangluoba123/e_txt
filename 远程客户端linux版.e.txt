 ' 文件类型：Linux控制台程序

 ' 程序名称：远程客户端LINUX版
 ' 程序描述：实现了在LINUX系统下，服务端与客户端的发送，处理数据功能。
 ' 程序作者：
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 4 2 系统核心支持库
 ' Exmlrpc A36CFD538657479eBD7C0D287BBB3D95 1 1 远程服务支持库





.程序集 启动程序集, , , 

.程序集变量 客户端, 未知类型0x20002, , , 
.程序集变量 请求, 逻辑型, , , 
.程序集变量 发送方式, 文本型, , , 
.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
连接服务器 ()

标准输入 ()
返回 (0) ' 可以根据您的需要返回任意数值

.子程序 连接服务器, 空白型, , 
.局部变量 端口号, 文本型, , , 
.局部变量 服务器地址, 文本型, , , 
.局部变量 请求方式, 文本型, , , 
.局部变量 连接, 逻辑型, , , 

标准输出 (相加 (“设置连接服务器”, #换行符))
标准输出 (“请输入端口号：”)
赋值 (端口号, 标准输入 ())
标准输出 (“请输入服务器地址：”)
赋值 (服务器地址, 标准输入 ())
标准输出 (“请设置请求方式：（0---同步请求，1---异步请求）”)
赋值 (请求方式, 标准输入 ())
.如果 (等于 (请求方式, “0”))
    赋值 (请求, 真)
    赋值 (连接, 客户端.未知支持库函数_15 (到数值 (端口号), 服务器地址, 真, ))
.否则
    赋值 (请求, 假)
    赋值 (连接, 客户端.未知支持库函数_15 (到数值 (端口号), 服务器地址, 假, &异步返回))
.如果结束
.如果 (等于 (连接, 真))
    标准输出 (相加 (“连接服务器成功”, #换行符))
.否则
    标准输出 (相加 (“连接服务器失败”, #换行符))
    连接服务器 ()
.如果结束

标准输出 (“请设置发送方式：（0---发送字节集，1---发送文本）”)
赋值 (发送方式, 标准输入 ())
标准输出 (相加 (“如果要断开连接请输入 dj-exit”, #换行符))
.如果 (等于 (发送方式, “1”))
    发送文本 ()
.否则
    发送字节集 ()
.如果结束


.子程序 异步返回, 空白型, , 
.参数 结果地址, 整数型, , 

.局部变量 结果文本, 文本型, , , 
.局部变量 结果字节集, 字节集, , , 

.如果 (等于 (发送方式, “1”))
    客户端.未知支持库函数_21 (结果地址, 结果文本)
    标准输出 (相加 (“返回：”, 结果文本, #换行符))
.否则
    客户端.未知支持库函数_22 (结果地址, 结果字节集)
    标准输出 (相加 (“返回：”, 到文本 (结果字节集), #换行符))
.如果结束


.子程序 发送字节集, 空白型, , 
.局部变量 结果字节集, 字节集, , , 
.局部变量 请求信息, 文本型, , , 
.局部变量 循环, 整数型, , , 

.判断循环首 (不等于 (循环, 1))
    标准输出 (“发送的字节集：”)
    赋值 (请求信息, 标准输入 ())
    .如果真 (等于 (删全部空 (请求信息), “dj-exit”))
        客户端.未知支持库函数_16 ()
        跳出循环 ()
    .如果真结束
    
    .如果 (等于 (请求, 真))
        客户端.未知支持库函数_18 (到字节集 (请求信息), 结果字节集, 5000)
        标准输出 (相加 (“返回”, 到文本 (结果字节集), #换行符))
        发送字节集 ()
    .否则
        客户端.未知支持库函数_20 (到字节集 (请求信息))
    .如果结束
    延时 (1000)
.判断循环尾 ()
标准输出 (相加 (“断开连接”, #换行符))
连接服务器 ()


.子程序 发送文本, 空白型, , 
.局部变量 结果文本, 文本型, , , 
.局部变量 请求信息, 文本型, , , 
.局部变量 循环, 整数型, , , 

.判断循环首 (不等于 (循环, 1))
    标准输出 (“发送的文本：”)
    赋值 (请求信息, 标准输入 ())
    .如果真 (等于 (删全部空 (请求信息), “dj-exit”))
        客户端.未知支持库函数_16 ()
        跳出循环 ()
    .如果真结束
    
    .如果 (等于 (请求, 真))
        客户端.未知支持库函数_17 (请求信息, 结果文本, 5000)
        标准输出 (相加 (“返回”, 结果文本, #换行符))
        发送字节集 ()
    .否则
        客户端.未知支持库函数_19 (请求信息)
    .如果结束
    延时 (1000)
.判断循环尾 ()
标准输出 (相加 (“断开连接”, #换行符))
连接服务器 ()



 ' 不属于任何一个程序集、类模块的函数：
