 ' 文件类型：Windows窗口程序

 ' 程序名称：
 ' 程序描述：
 ' 程序作者：
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0

窗口 _启动窗口 ' 在程序启动后自动调入本窗口
    左边 = 50
    顶边 = 50
    宽度 = 380
    高度 = 279
    鼠标指针 = { 0,0,0,0 }
    标记 = “”
    可视 = 真
    禁止 = 假





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 4 4 系统核心支持库
 ' iext 27bb20fdd3e145e4bee3db39ddd6e64c 1 2 扩展界面支持库一
 ' eExcel2000 F86EC5989E044d42BC98C692C0B54727 1 0 EXCEL2000支持库




.全局变量 mp3组, 文本型, , "0", 
.全局变量 随机mp3组, 文本型, , "0", 
.全局变量 匿名全局变量_26, 空白型, , , 

.DLL命令 捕获鼠标, 整数型, "", "SetCapture", , 
    .参数 句柄, 整数型, , 接收所有鼠标输入的窗口的句柄

.DLL命令 释放鼠标, 整数型, "", "ReleaseCapture", , 

.窗口程序集 窗口程序集1, , , 

.子程序 _添加文件_被选择, 空白型, , 
.局部变量 变量, 整数型, , , 
.局部变量 临时mp3组, 文本型, , "0", 

赋值 (临时mp3组, 多文件对话框 (“请选择要添加的MP3文件...”, “文本文件|*”, , , ))
.如果真 (等于 (取数组成员数 (临时mp3组), 0))
    返回 ()
.如果真结束
.计次循环首 (取数组成员数 (临时mp3组), 变量)
    _启动窗口.插入表项 (., -1, 临时mp3组 [变量], , , , )
    加入成员 (mp3组, 临时mp3组 [变量])
    加入成员 (随机mp3组, 临时mp3组 [变量])
.计次循环尾 ()
赋值 (.显示方式, 1)
赋值 (.显示方式, 1)

.子程序 _按钮1_被单击, 空白型, , 
_添加文件_被选择 ()

.子程序 _按钮2_被单击, 空白型, , 
.局部变量 文件名, 文本型, , , 
.局部变量 文件号, 整数型, , , 
.局部变量 文本, 文本型, , , 
.局部变量 变量, 整数型, , , 
.局部变量 临时容器A, 文本型, , "0", 
.局部变量 临时容器B, 整数型, , , 
.局部变量 行次, 整数型, , , 

未知支持库函数_0 ()
赋值 (., 真)
.计次循环首 (取表项数 (), 变量)
    赋值 (文件名, 相减 (变量, 1).取标题 ())
    赋值 (文件号, 打开文件 (文件名, , ))
    移到文件首 (文件号)
    赋值 (.位置, 相加 (.位置, 相除 (100, 取表项数 ())))
    .未知支持库函数_6 ()
    赋值 (., 1)
    赋值 (行次, 1)
    .判断循环首 (取反 (是否在文件尾 (文件号, )))
        赋值 (文本, 读入一行 (文件号))
        赋值 (.位置, 相加 (.位置, 1))
        .如果真 (不等于 (删首尾空 (文本), “”))
            .如果 (大于 (寻找文本 (文本, .内容, 0, 真), 0))
                赋值 (临时容器A, 分割文本 (文本, .内容, ))
                加入成员 (临时容器A, 文本)
                .变量循环首 (1, 相减 (取数组成员数 (临时容器A), 1), 1, 临时容器B)
                     ' 信息框 (字符 (64 ＋ 临时容器B) ＋ 到文本 (行次 ＋ 3), 0, )
                    .如果 (等于 (1))
                        赋值 (., 相加 (字符 (相加 (64, )), 到文本 (相加 (2))))
                        赋值 (., 相加 (字符 (相加 (64, )), 到文本 (相加 (2))))
                        赋值 (., -4108)
                        赋值 (., 相加 (“字段”, 到文本 ()))
                        赋值 (., “黑体”)
                        赋值 (., 取文本长度 (临时容器A [临时容器B]))
                        1.未知支持库函数_30 (2, #黑色)
                    .否则
                        
                    .如果结束
                    赋值 (., 相加 (字符 (相加 (64, 临时容器B)), 到文本 (相加 (行次, 3))))
                    赋值 (., 相加 (字符 (相加 (64, 临时容器B)), 到文本 (相加 (行次, 3))))
                    1.未知支持库函数_30 (2, #黑色)
                    
                     ' 信息框 (到文本 (取文本长度 (临时容器A [临时容器B])), 0, )
                    .如果 (不等于 (取文本长度 (临时容器A [临时容器B]), 23))
                        赋值 (., 相加 (“'”, 删首尾空 (临时容器A [临时容器B])))
                    .否则
                        赋值 (., 临时容器A [临时容器B])
                    .如果结束
                    
                .变量循环尾 ()
            .否则
                
            .如果结束
            
        .如果真结束
        赋值 (行次, 相加 (行次, 1))
    .判断循环尾 ()
    赋值 (., “a1”)
    赋值 (., 相加 (字符 (相减 (相加 (64, ), 1)), “1”))
    未知支持库函数_26 ()
    赋值 (., 40)
    赋值 (., “易语言多文本转EXCEL”)
    赋值 (., “隶书”)
    赋值 (., 18)
    赋值 (., -4108)
    赋值 (.类型, 1)
    赋值 (.标题, “保存Excel文件”)
    文件名.未知支持库函数_16 ()
    未知支持库函数_15 ()
.计次循环尾 ()



 ' 不属于任何一个程序集、类模块的函数：
