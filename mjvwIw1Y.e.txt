 ' 文件类型：Windows模块源码

 ' 程序名称：模块_找子字节集
 ' 程序描述：适用于被找字节集中包含很多要找的字节集的情况使用！
 ' 程序作者：dtcser
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 4 4 系统核心支持库





.程序集 程序集1, , , 

.子程序 _启动子程序, 整数型, , 请在本子程序中放置易模块初始化代码

_临时子程序 () ' 在初始化代码执行完毕后调用测试代码
返回 (0) ' 可以根据您的需要返回任意数值

.子程序 _临时子程序, 空白型, , 
 ' 本名称子程序用作测试程序用，仅在开发及调试环境中有效，编译发布程序前将被系统自动清空，请将所有用作测试的临时代码放在本子程序中。 ***注意不要修改本子程序的名称、参数及返回值类型。


.子程序 kmp_init, 空白型, , 
.参数 b, 字节集, , 
.参数 p, 整数型, 数组, 

.局部变量 i, 空白型, , , 
.局部变量 j, 空白型, , , 
.局部变量 m, 空白型, , , 

赋值 (m, 取字节集长度 (b))
重定义数组 (p, 假, m)
赋值 (p [1], 0)
.变量循环首 (2, m, 1, i)
    .判断循环首 (并且 (大于 (j, 0), 不等于 (b [相加 (j, 1)], b [i])))
        赋值 (j, p [j])
    .判断循环尾 ()
    .如果真 (等于 (b [相加 (j, 1)], b [i]))
        赋值 (j, 相加 (j, 1))
    .如果真结束
    赋值 (p [i], j)
.变量循环尾 ()


.子程序 kmp_find, 空白型, , 
.参数 a, 字节集, , 
.参数 b, 字节集, , 
.参数 p, 整数型, 数组, 
.参数 x, 整数型, 数组, 

.局部变量 i, 空白型, , , 
.局部变量 j, 空白型, , , 
.局部变量 n, 空白型, , , 
.局部变量 m, 空白型, , , 

赋值 (n, 取字节集长度 (a))
赋值 (m, 取字节集长度 (b))
.计次循环首 (n, i)
    .判断循环首 (并且 (大于 (j, 0), 不等于 (b [相加 (j, 1)], a [i])))
        赋值 (j, p [j])
    .判断循环尾 ()
    .如果真 (等于 (b [相加 (j, 1)], a [i]))
        赋值 (j, 相加 (j, 1))
    .如果真结束
    .如果真 (等于 (j, m))
        加入成员 (x, 相加 (相减 (i, m), 1))
        赋值 (j, p [j])
    .如果真结束
    
.计次循环尾 ()


.子程序 字节集_子字节集寻找, 整数型, , 错误返回-1,否则返回找到的个数,位置保存在“结果保存数组中”
.参数 被搜寻的字节集, 字节集, , 
.参数 欲寻找的字节集, 字节集, , 
.参数 结果保存数组, 整数型, 数组, 用来保存所找到字节集的位置

.局部变量 返回值, 整数型, , , 
.局部变量 next, 整数型, , "0", 
.局部变量 a, 空白型, , , 
.局部变量 b, 空白型, , , 

赋值 (a, 取字节集长度 (被搜寻的字节集))
赋值 (b, 取字节集长度 (欲寻找的字节集))
.如果真 (或者 (小于 (a, 1), 小于 (b, 1), 小于 (a, b)))
    返回 (-1)
.如果真结束
清除数组 (结果保存数组)
kmp_init (欲寻找的字节集, next)
kmp_find (被搜寻的字节集, 欲寻找的字节集, next, 结果保存数组)
返回 (取数组成员数 (结果保存数组))



 ' 不属于任何一个程序集、类模块的函数：
