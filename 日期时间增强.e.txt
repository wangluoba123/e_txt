 ' 文件类型：Windows窗口程序

 ' 程序名称：增强时间显示 v1.0
 ' 程序描述：处理转换日期时间的显示。
 ' 程序作者：钟少敏 [寻梦]
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：luckkk@tom.com
 ' 主页地址：
 ' 版权声明：多多支持我吧！
 ' 版本号：1.0
 ' 创建号：0.0





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 3 2 系统核心支持库





.程序集 程序集1, , , 

.子程序 _启动子程序, 整数型, , 请在本子程序中放置易模块初始化代码

_临时子程序 () ' 在模块初始化代码执行完毕后调用模块测试代码

.子程序 _临时子程序, 空白型, , 
 ' 本名称子程序用作测试程序用，仅在开发及调试环境中有效，编译发布程序前将被系统自动清空，请将所有用作测试的临时代码放在本子程序中。 ***注意不要修改本子程序的参数及返回值类型。

.程序集 _接口程序集, , , 在本程序集中定义的子程序可被模块外部程序访问，注意如果不存在本程序集就不能作为易模块编译。

.子程序 转换启动时间, 日期时间型, , 将启动时间转换成时间格式。（启动时间在24小时以内）
.参数 启动时间, 整数型, , 单位：秒

返回 ()

.子程序 星期几, 文本型, , 
.参数 日期, 日期时间型, , 

.局部变量 星期几, 整数型, , , 

赋值 (星期几, 取星期几 (日期))
.判断开始 (等于 (星期几, 1))
    返回 (“星期日”)
.判断 (等于 (星期几, 2))
    返回 (“星期一”)
.判断 (等于 (星期几, 3))
    返回 (“星期二”)
.判断 (等于 (星期几, 4))
    返回 (“星期三”)
.判断 (等于 (星期几, 5))
    返回 (“星期四”)
.判断 (等于 (星期几, 6))
    返回 (“星期五”)
.默认
    返回 (“星期六”)
.判断结束


.子程序 增强日期, 文本型, , 2003-11-6　变成　2003-11-06
.参数 时间, 日期时间型, , 
.参数 日期分隔符, 文本型, 可空, 默认为：年、月、日

.局部变量 月文本, 文本型, , , 
.局部变量 日文本, 文本型, , , 

.如果 (小于 (取月份 (时间), 10))
    赋值 (月文本, 相加 (“0”, 到文本 (取月份 (时间))))
.否则
    赋值 (月文本, 到文本 (取月份 (时间)))
.如果结束
.如果 (小于 (取日 (时间), 10))
    赋值 (日文本, 相加 (“0”, 到文本 (取日 (时间))))
.否则
    赋值 (日文本, 到文本 (取日 (时间)))
.如果结束
.如果 (等于 (日期分隔符, “”))
    返回 (相加 (到文本 (取年份 (时间)), “年”, 月文本, “月”, 日文本, “日”))
.否则
    返回 (相加 (到文本 (取年份 (时间)), 日期分隔符, 月文本, 日期分隔符, 日文本))
.如果结束


.子程序 增强时间, 文本型, , 17:2:9　变成　17:02:09
.参数 时间, 日期时间型, , 
.参数 样式, 字节型, 可空, 显示时间的样式。无参数，17:20:09；参数1，12时08分32秒；参数2，PM 01:20:09

.局部变量 时, 字节型, , , 
.局部变量 时文本, 文本型, , , 
.局部变量 分文本, 文本型, , , 
.局部变量 秒文本, 文本型, , , 

.如果 (小于 (取小时 (时间), 10))
    赋值 (时文本, 相加 (“0”, 到文本 (取小时 (时间))))
.否则
    赋值 (时文本, 到文本 (取小时 (时间)))
.如果结束
.如果 (小于 (取分钟 (时间), 10))
    赋值 (分文本, 相加 (“0”, 到文本 (取分钟 (时间))))
.否则
    赋值 (分文本, 到文本 (取分钟 (时间)))
.如果结束
.如果 (小于 (取秒 (时间), 10))
    赋值 (秒文本, 相加 (“0”, 到文本 (取秒 (时间))))
.否则
    赋值 (秒文本, 到文本 (取秒 (时间)))
.如果结束
.判断开始 (等于 (样式, 1))
    返回 (相加 (时文本, “时”, 分文本, “分”, 秒文本, “秒”))
.判断 (等于 (样式, 2))
    赋值 (时, 取小时 (时间))
    .如果 (大于 (时, 12))
        赋值 (时, 相减 (时, 12))
        .判断开始 (小于 (时, 10))
            赋值 (时文本, 相加 (“0”, 到文本 (时)))
            返回 (相加 (时文本, “:”, 分文本, “:”, 秒文本))
        .默认
            赋值 (时文本, 到文本 (时))
            返回 (相加 (时文本, “:”, 分文本, “:”, 秒文本))
        .判断结束
        
    .否则
        返回 (相加 (时文本, “:”, 分文本, “:”, 秒文本))
    .如果结束
    
.默认
    返回 (相加 (时文本, “:”, 分文本, “:”, 秒文本))
.判断结束



 ' 不属于任何一个程序集、类模块的函数：
