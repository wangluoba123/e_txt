 ' 文件类型：Windows模块源码

 ' 程序名称：文本数组排序
 ' 程序描述：排序结果存放回该数组变量中。
 ' 程序作者：amdoy
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：排序结果存放回该数组变量中。
 ' 版本号：1.0
 ' 创建号：0.0





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 5 0 系统核心支持库





.程序集 程序集1, , , 

.子程序 _启动子程序, 整数型, , 请在本子程序中放置易模块初始化代码

_临时子程序 () ' 在初始化代码执行完毕后调用测试代码
返回 (0) ' 可以根据您的需要返回任意数值

.子程序 _临时子程序, 空白型, , 
 ' 本名称子程序用作测试程序用，仅在开发及调试环境中有效，编译发布程序前将被系统自动清空，请将所有用作测试的临时代码放在本子程序中。 ***注意不要修改本子程序的名称、参数及返回值类型。


.子程序 文本数组排序, 空白型, 公开, 支持单维文本型数组
.参数 文本数组, 文本型, 数组, 
.参数 从大到小, 逻辑型, 可空, 默认为:假(即从小到大)，真:从大的小

.局部变量 计次, 整数型, , , 
.局部变量 操作数组, 文本型, , "0", 
.局部变量 高位, 整数型, , , 
.局部变量 低位, 整数型, , , 
.局部变量 N, 整数型, , , 
.局部变量 M, 整数型, , , 
.局部变量 中间位, 整数型, , , 

复制数组 (操作数组, 文本数组)
清除数组 (文本数组)
赋值 (M, 取数组成员数 (操作数组))
.如果真 (等于 (M, 0))
    返回 ()
.如果真结束
.计次循环首 (M, 计次)
    .如果 (等于 (N, 0))
        插入成员 (文本数组, 1, 操作数组 [计次])
    .否则
        赋值 (高位, N)
        赋值 (低位, 1)
        赋值 (中间位, 整除 (相加 (高位, 低位), 2))
        .如果 (等于 (从大到小, 假)) ' 从小到大
            .如果 (大于或等于 (操作数组 [计次], 文本数组 [N]))
                插入成员 (文本数组, 相加 (N, 1), 操作数组 [计次])
            .否则
                .如果 (小于或等于 (操作数组 [计次], 文本数组 [1]))
                    插入成员 (文本数组, 1, 操作数组 [计次])
                .否则
                    .判断循环首 (大于 (相减 (高位, 低位), 1))
                        .如果真 (等于 (操作数组 [计次], 文本数组 [中间位]))
                            跳出循环 ()
                        .如果真结束
                        .如果 (小于 (操作数组 [计次], 文本数组 [中间位]))
                            赋值 (高位, 中间位)
                        .否则
                            赋值 (低位, 中间位)
                        .如果结束
                        赋值 (中间位, 整除 (相加 (高位, 低位), 2))
                    .判断循环尾 ()
                    .如果 (小于或等于 (操作数组 [计次], 文本数组 [中间位]))
                        插入成员 (文本数组, 中间位, 操作数组 [计次])
                    .否则
                        插入成员 (文本数组, 相加 (中间位, 1), 操作数组 [计次])
                    .如果结束
                    
                .如果结束
                
            .如果结束
            
        .否则
             ' 从大的小
            .如果 (大于或等于 (操作数组 [计次], 文本数组 [1]))
                插入成员 (文本数组, 1, 操作数组 [计次])
            .否则
                .如果 (小于或等于 (操作数组 [计次], 文本数组 [N]))
                    插入成员 (文本数组, 相加 (N, 1), 操作数组 [计次])
                .否则
                    .判断循环首 (大于 (相减 (高位, 低位), 1))
                        .如果真 (等于 (操作数组 [计次], 文本数组 [中间位]))
                            跳出循环 ()
                        .如果真结束
                        .如果 (大于 (操作数组 [计次], 文本数组 [中间位]))
                            赋值 (高位, 中间位)
                        .否则
                            赋值 (低位, 中间位)
                        .如果结束
                        赋值 (中间位, 整除 (相加 (高位, 低位), 2))
                    .判断循环尾 ()
                    .如果 (大于或等于 (操作数组 [计次], 文本数组 [中间位]))
                        插入成员 (文本数组, 中间位, 操作数组 [计次])
                    .否则
                        插入成员 (文本数组, 相加 (中间位, 1), 操作数组 [计次])
                    .如果结束
                    
                .如果结束
                
            .如果结束
            
        .如果结束
        
    .如果结束
    赋值 (N, 取数组成员数 (文本数组))
.计次循环尾 ()



 ' 不属于任何一个程序集、类模块的函数：
