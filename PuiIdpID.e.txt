 ' 文件类型：Windows窗口程序

 ' 程序名称：
 ' 程序描述：
 ' 程序作者：
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0

窗口 _启动窗口 ' 在程序启动后自动调入本窗口
    左边 = 50
    顶边 = 50
    宽度 = 396
    高度 = 226
    鼠标指针 = { 0,0,0,0 }
    标记 = “”
    可视 = 真
    禁止 = 假
    边框 = 2
    底图方式 = 1
    底色 = -16777216 '  0xFF000000
    最大化按钮 = 假
    最小化按钮 = 假
    控制按钮 = 真
    位置 = 1
    可否移动 = 真
    背景音乐播放次数 = 0
    回车下移焦点 = 假
    Esc键关闭 = 真
    F1键打开帮助 = 假
    帮助标志值 = 0
    在任务栏中显示 = 真
    随意移动 = 假
    外形 = 0
    总在最前 = 假
    保持标题栏激活 = 假
    底图 =  ' 空
    图标 =  ' 空
    背景音乐 =  ' 空
    标题 = “风很凉-图片浮雕效果”
    帮助文件名 = “”





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 4 5 系统核心支持库





.窗口程序集 窗口程序集1, , , 

.子程序 _按钮1_被单击, 空白型, , 
.局部变量 r2, 空白型, , , 
.局部变量 g2, 空白型, , , 
.局部变量 b2, 空白型, , , 
.局部变量 r1, 空白型, , , 
.局部变量 g1, 空白型, , , 
.局部变量 b1, 空白型, , , 
.局部变量 c1, 空白型, , , 
.局部变量 c2, 空白型, , , 
.局部变量 x0, 空白型, , , 
.局部变量 y0, 空白型, , , 

.计次循环首 (相减 (., 2), x0)
    .计次循环首 (相减 (., 2), y0)
        赋值 (c1, x0.取点 (y0))
        赋值 (r1, 位与 (c1, 255))
        赋值 (g1, 相除 (位与 (c1, 62580), 256)) ' ;;;
        赋值 (b1, 相除 (位与 (c1, 16711680), 65536))
         ' 获得Picture1中指定点(x0,y0)的R、G、B分量值
        赋值 (c2, 相加 (x0, 1).取点 (相加 (y0, 1)))
        赋值 (r2, 位与 (c2, 255))
        赋值 (g2, 相除 (位与 (c2, 62580), 256)) ' ;;;;
        赋值 (b2, 相除 (位与 (c2, 16711680), 65536))
         ' 获得Picture1中与(x0,y0)点相邻的点的R、G、B分量值
        赋值 (r1, 取绝对值 (相加 (相减 (r1, r2), 128)))
        赋值 (g1, 取绝对值 (相加 (相减 (g1, g2), 128)))
        赋值 (b1, 取绝对值 (相加 (相减 (b1, b2), 128)))
        .如果真 (大于 (r1, 255))
            赋值 (r1, 255)
        .如果真结束
        .如果真 (小于 (r1, 0))
            赋值 (r1, 0)
        .如果真结束
        .如果真 (大于 (b1, 255))
            赋值 (b1, 255)
        .如果真结束
        .如果真 (小于 (b1, 0))
            赋值 (b1, 0)
        .如果真结束
        .如果真 (大于 (g1, 255))
            赋值 (g1, 255)
        .如果真结束
        .如果真 (小于 (g1, 0))
            赋值 (g1, 0)
        .如果真结束
        x0.画点 (y0, 取颜色值 (r1, g1, b1))
    .计次循环尾 ()
    处理事件 ()
.计次循环尾 ()






 ' 不属于任何一个程序集、类模块的函数：
