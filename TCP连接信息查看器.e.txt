 ' 文件类型：Windows窗口程序

 ' 程序名称：Tcp连接信息查看器
 ' 程序描述：
 ' 程序作者：金眼睛
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：goldeye2000@163.com
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0

窗口 _启动窗口 ' 在程序启动后自动调入本窗口
    左边 = 50
    顶边 = 50
    宽度 = 449
    高度 = 250
    鼠标指针 = { 0,0,0,0 }
    标记 = “”
    可视 = 真
    禁止 = 假
    边框 = 2
    底图方式 = 1
    底色 = -16777216 '  0xFF000000
    最大化按钮 = 假
    最小化按钮 = 假
    控制按钮 = 真
    位置 = 1
    可否移动 = 真
    背景音乐播放次数 = 0
    回车下移焦点 = 假
    Esc键关闭 = 真
    F1键打开帮助 = 假
    帮助标志值 = 0
    在任务栏中显示 = 真
    随意移动 = 假
    外形 = 0
    总在最前 = 假
    保持标题栏激活 = 假
    底图 =  ' 空
    图标 =  ' 空
    背景音乐 =  ' 空
    标题 = “TCP连接信息查看器”
    帮助文件名 = “”





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 3 7 系统核心支持库
 ' iext 27bb20fdd3e145e4bee3db39ddd6e64c 1 1 扩展界面支持库一




数据类型 TCP记录, , 
    .成员 状态, 文本型, , , 
    .成员 本机地址, 文本型, , , 
    .成员 本机端口, 文本型, , , 
    .成员 远程地址, 文本型, , , 
    .成员 远程端口, 文本型, , , 


.DLL命令 取TCP列表, 整数型, "iphlpapi.dll", "GetTcpTable", , 
    .参数 列表, 字节集, 传址, 
    .参数 长度, 整数型, 传址, 
    .参数 命令, 整数型, , 

.窗口程序集 窗口程序集1, , , 

.子程序 取状态, 文本型, , 
.参数 参数, 整数型, , 

.判断开始 (等于 (参数, 1))
    返回 (“已关闭”)
.判断 (等于 (参数, 2))
    返回 (“正在监听”)
.判断 (等于 (参数, 3))
    返回 (“同步发送”)
.判断 (等于 (参数, 4))
    返回 (“同步接收”)
.判断 (等于 (参数, 5))
    返回 (“已建立”)
.判断 (等于 (参数, 6))
    返回 (“FIN_WAIT1”)
.判断 (等于 (参数, 7))
    返回 (“FIN_WAIT2”)
.判断 (等于 (参数, 8))
    返回 (“关闭等待”)
.判断 (等于 (参数, 9))
    返回 (“正在关闭”)
.判断 (等于 (参数, 10))
    返回 (“最后确认”)
.判断 (等于 (参数, 11))
    返回 (“延时等待”)
.判断 (等于 (参数, 12))
    返回 (“删除”)
.默认
    返回 (“未知”)
.判断结束


.子程序 _按钮1_被单击, 空白型, , 
.局部变量 缓存尺寸, 整数型, , , 
.局部变量 连接数目, 整数型, , , 
.局部变量 整数容器, 整数型, , , 
.局部变量 TCP信息, 字节集, , , 
.局部变量 TCP记录表, 字节集, , , 
.局部变量 TCP记录, TCP记录, , "0", 
.局部变量 起始位, 整数型, , , 
.局部变量 列表索引值, 整数型, , , 

全部删除 ()
取TCP列表 ({ 0 }, 缓存尺寸, 1) ' 查询所需缓存尺寸大小
.如果真 (大于 (缓存尺寸, 0))
    赋值 (TCP信息, 取空白字节集 (缓存尺寸)) ' 分配缓存空间
    .如果真 (等于 (取TCP列表 (TCP信息, 缓存尺寸, 1), 0)) ' 0:获取成功  87：参数错误  50：系统不支持  111：缓存溢出  232：无数据
        赋值 (连接数目, 取字节集数据 (取字节集中间 (TCP信息, 1, 4), #整数型))
        赋值 (TCP记录表, 取字节集右边 (TCP信息, 相减 (缓存尺寸, 4)))
        重定义数组 (TCP记录, 假, 连接数目)
        .计次循环首 (连接数目, 整数容器)
            赋值 (起始位, 相乘 (相减 (整数容器, 1), 20))
            赋值 (TCP记录 [整数容器], 状态, 取状态 (取字节集数据 (取字节集中间 (TCP记录表, 相加 (起始位, 1), 4), #整数型)))
            赋值 (TCP记录 [整数容器], 本机地址, 相加 (到文本 (TCP记录表 [相加 (起始位, 5)]), “.”, 到文本 (TCP记录表 [相加 (起始位, 6)]), “.”, 到文本 (TCP记录表 [相加 (起始位, 7)]), “.”, 到文本 (TCP记录表 [相加 (起始位, 8)])))
            赋值 (TCP记录 [整数容器], 本机端口, 到文本 (相加 (相乘 (TCP记录表 [相加 (起始位, 9)], 256), TCP记录表 [相加 (起始位, 10)])))
            赋值 (TCP记录 [整数容器], 远程地址, 相加 (到文本 (TCP记录表 [相加 (起始位, 13)]), “.”, 到文本 (TCP记录表 [相加 (起始位, 14)]), “.”, 到文本 (TCP记录表 [相加 (起始位, 15)]), “.”, 到文本 (TCP记录表 [相加 (起始位, 16)])))
            赋值 (TCP记录 [整数容器], 远程端口, 到文本 (相加 (相乘 (TCP记录表 [相加 (起始位, 17)], 256), TCP记录表 [相加 (起始位, 18)])))
            .如果真 (并且 (不等于 (TCP记录 [整数容器], 本机地址, “0.0.0.0”), 不等于 (TCP记录 [整数容器], 本机地址, “127.0.0.1”), 不等于 (TCP记录 [整数容器], 远程地址, “0.0.0.0”))) ' 过滤本机环回连接信息
                赋值 (列表索引值, .插入表项 (TCP记录 [整数容器], 本机地址, , , , ))
                列表索引值.置标题 (1, TCP记录 [整数容器], 本机端口)
                列表索引值.置标题 (2, TCP记录 [整数容器], 远程地址)
                列表索引值.置标题 (3, TCP记录 [整数容器], 远程端口)
                列表索引值.置标题 (4, TCP记录 [整数容器], 状态)
            .如果真结束
            
        .计次循环尾 ()
    .如果真结束
    
.如果真结束



 ' 不属于任何一个程序集、类模块的函数：
