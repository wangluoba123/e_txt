 ' 文件类型：Windows窗口程序

 ' 程序名称：
 ' 程序描述：
 ' 程序作者：
 ' 邮政编码：
 ' 联系地址：
 ' 联系电话：
 ' 传真号码：
 ' 电子信箱：
 ' 主页地址：
 ' 版权声明：
 ' 版本号：1.0
 ' 创建号：0.0

窗口 _启动窗口 ' 在程序启动后自动调入本窗口
    左边 = 50
    顶边 = 50
    宽度 = 380
    高度 = 250
    鼠标指针 = { 0,0,0,0 }
    标记 = “”
    可视 = 真
    禁止 = 假





 ' 所需要的支持库：
 ' krnln d09f2340818511d396f6aaf844c7e325 4 4 系统核心支持库





.DLL命令 CallWindowProcA, 整数型, "", "CallWindowProcA", , 
    .参数 lpPrevWndFunc, 字节集, , 
    .参数 hwnd, 空白型, , 
    .参数 msg, 空白型, , 
    .参数 wParam, 空白型, , 
    .参数 iparam, 空白型, , 

.窗口程序集 窗口程序集1, , , 

.子程序 __启动窗口_创建完毕, 空白型, , 
赋值 (., 取系统类型 ())

.子程序 取系统类型, 文本型, , 
.局部变量 peb, 空白型, , , 
.局部变量 teb, 空白型, , , 
.局部变量 OSMajorVersion, 空白型, , , 
.局部变量 OSMinorVersion, 空白型, , , 

赋值 (peb, CallWindowProcA ({ 85, 139, 236, 100, 161, 24, 0, 0, 0, 139, 64, 48, 139, 229, 93, 195 }, 0, 0, 0, 0))
赋值 (teb, CallWindowProcA ({ 85, 139, 236, 100, 161, 24, 0, 0, 0, 139, 229, 93, 195 }, 0, 0, 0, 0))

.如果 (并且 (等于 (取文本左边 (取十六进制文本 (peb), 4), “7FFD”), 等于 (取文本左边 (取十六进制文本 (teb), 4), “7FFD”)))
     ' nt类型
    赋值 (OSMinorVersion, 取字节集数据 (指针到字节集 (相加 (peb, 168), 4), #整数型, ))
    赋值 (OSMajorVersion, 取字节集数据 (指针到字节集 (相加 (peb, 164), 4), #整数型, ))
    .如果 (等于 (OSMajorVersion, 5))
        .判断开始 (等于 (OSMinorVersion, 0))
            返回 (“Windows 2000”)
        .判断 (等于 (OSMinorVersion, 1))
            返回 (“Windows XP”)
        .判断 (等于 (OSMinorVersion, 2))
            返回 (“Windows 2003”)
        .默认
            
        .判断结束
        
    .否则
        返回 (“Windows NT”)
    .如果结束
    
.否则
    .如果真 (等于 (取字节集数据 (指针到字节集 (相加 (teb, 84), 4), #整数型, ), 5439488))
        返回 (“Windows 98”)
    .如果真结束
    .如果真 (等于 (取字节集数据 (指针到字节集 (相加 (teb, 88), 4), #整数型, ), 5439488))
        返回 (“Windows 95”)
    .如果真结束
    .如果真 (等于 (取字节集数据 (指针到字节集 (相加 (teb, 124), 4), #整数型, ), 5439488))
        返回 (“Windows ME”)
    .如果真结束
    
.如果结束
返回 (“UNKNOWN”)



 ' 不属于任何一个程序集、类模块的函数：
